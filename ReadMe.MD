clone the repo into one of your bash path locations [i.e. c:/users/uname/bin]

enjoy the commands by typing:
git <filename-without-git->

ex:
filename: git-cleanuplocals
type:
git cleanuplocals
